import { InjectionToken } from '@angular/core';

/** Injection token for Firestore offline persistence */
export const EnablePersistenceToken = new InjectionToken<boolean>('AngularFirestype.EnablePersistenceToken');
